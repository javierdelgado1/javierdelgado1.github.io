{
  "name": "javierdelgado-blog",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Blog estático de Javier Delgado. Genera dist/ a partir de layout/ + posts/*.md y lo publica por FTP en un hosting cPanel.",
  "author": "ingeniero@javierdelgado.com.ve",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && node serve.js",
    "new": "node new-post.js",
    "clean": "rm -rf dist",
    "deploy": "bash deploy.sh",
    "deploy:dry": "bash deploy.sh --dry-run"
  },
  "dependencies": {
    "basic-ftp": "6.2.1",
    "marked": "18.0.13"
  }
}
